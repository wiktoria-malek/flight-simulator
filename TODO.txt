# restore button
# threshold doesnt work
# create 3 panels inside bba gui for the plots
# display test orbits
# add red button to reset the reference orbit in the gui
# OTR => Interface
# Emittance Measurements
# display dispersion/wakefield, display response response matrices, including r1-r0, r2-r0
# in computeresponsematrix_gui do the differences between two directories
# introduce beta function to the correction
# remove the choice of the atf2/clear and maybe see if theres a directory called ATF2 and Clear -
    if not, display only choice of the inetrfaces
# sysid after hitting 100% should close automatically
# we can try to make the scripts executable via shell not inputting every time
# readback set value until it reaches the exact value, it would be faster than sleeping 1 second while doing bba i think
# add abstract classes to state.py
# in sysid, you have to click stop several times when in all modes mode in order to stop the gui
# why does sometimes it works and sometimes doesn't? what happens when there's nothing to correct
# for screeens: with bpms right now, it looks like we have name,data and it stacks with bpms, creating one big matrix.
    maybe it makes sense to- with screens, operate with data structures that is more divided? like, screen - images etc?
# remove the redundancy
# saving session settings after appending points to the gui
# we need to do something like -> interface(state) and then so it can directly call pull, not to just reference a state
# we can delete those return get bpms names etc because maybe there is a way to actually access them
# in state there will be also quadrupole strength getting, for emittance measurement/scan etc
# we can add the beta parameter